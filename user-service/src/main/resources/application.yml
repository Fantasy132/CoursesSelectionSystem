server:
  port: 8100

spring:
  application:
    name: user-service
  cloud:
    compatibility-verifier:
      enabled: false  # 禁用版本兼容性检查
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER_ADDR:localhost:8848}
        namespace: dev
        group: COURSEHUB_GROUP
        ephemeral: true
        heart-beat-interval: 5000
        heart-beat-timeout: 15000
        enabled: ${NACOS_ENABLED:true}
        username: nacos
        password: nacos
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:33061/user_db?useSSL=false&serverTimezone=UTC}
    username: ${SPRING_DATASOURCE_USERNAME:user_user}
    password: ${SPRING_DATASOURCE_PASSWORD:user_pass}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

# 课程目录服务配置（使用 Nacos 服务发现）
catalog-service:
  name: catalog-service  # Nacos 中的服务名